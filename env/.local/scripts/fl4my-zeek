FROM zeek/zeek:latest

RUN apt-get update && apt-get install -y less \
  vim \
  jq \
  curl \
  file \
  && rm -rf /var/lib/apt/lists/*

WORKDIR /mnt 

# to run docker zeek use this command:
# docker run -it --rm -v $(pwd):/mnt -w /mnt fl4my-zeek bash
